name: hello-world-workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: Hello world
        run: echo "Hello World, I am Saurabh Kumar"